<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jonlink.system.mapper.JonlinkChannelUserMapper">

    <resultMap type="JonlinkChannelUser" id="JonlinkChannelUserResult">
        <result property="id"        column="id"         />
        <result property="userName"  column="user_name"  />
        <result property="phone"     column="phone"      />
        <result property="userType"  column="user_type"  />
        <result property="status"    column="status"     />
        <result property="remark"    column="remark"     />
        <result property="createBy"  column="create_by"  />
        <result property="createTime" column="create_time" />
        <result property="updateBy"  column="update_by"  />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectJonlinkChannelUserVo">
        select id, user_name, phone, user_type, status, remark, create_by, create_time, update_by, update_time
        from jonlink_channel_user
    </sql>

    <select id="selectJonlinkChannelUserList" parameterType="JonlinkChannelUser" resultMap="JonlinkChannelUserResult">
        <include refid="selectJonlinkChannelUserVo"/>
        <where>
            <if test="userName != null and userName != ''">
                AND user_name LIKE concat('%', #{userName}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone LIKE concat('%', #{phone}, '%')
            </if>
            <if test="userType != null and userType != ''">
                AND user_type = #{userType}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="selectJonlinkChannelUserById" parameterType="Long" resultMap="JonlinkChannelUserResult">
        <include refid="selectJonlinkChannelUserVo"/>
        WHERE id = #{id}
    </select>

    <!-- 台账渠道类型=2 下拉数据源:仅返回启用(id+user_name) -->
    <select id="selectJonlinkChannelUserOptions" resultMap="JonlinkChannelUserResult">
        SELECT id, user_name FROM jonlink_channel_user WHERE status = '1' ORDER BY id DESC
    </select>

    <insert id="insertJonlinkChannelUser" parameterType="JonlinkChannelUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO jonlink_channel_user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userName != null and userName != ''">user_name,</if>
            <if test="phone != null">phone,</if>
            <if test="userType != null and userType != ''">user_type,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null">remark,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userName != null and userName != ''">#{userName},</if>
            <if test="phone != null">#{phone},</if>
            <if test="userType != null and userType != ''">#{userType},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null">#{remark},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateJonlinkChannelUser" parameterType="JonlinkChannelUser">
        UPDATE jonlink_channel_user
        <trim prefix="SET" suffixOverrides=",">
            <if test="userName != null and userName != ''">user_name = #{userName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="userType != null and userType != ''">user_type = #{userType},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        WHERE id = #{id}
    </update>

    <delete id="deleteJonlinkChannelUserById" parameterType="Long">
        DELETE FROM jonlink_channel_user WHERE id = #{id}
    </delete>

    <delete id="deleteJonlinkChannelUserByIds" parameterType="String">
        DELETE FROM jonlink_channel_user WHERE id IN
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
